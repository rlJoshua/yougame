<nav class="navbar navbar-expand-lg navbar-light bg-dark">
    <a class="navbar-brand" href="{{ path('home') }}">YouGame</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText"
            aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('list_game') }}">Jeux</a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('list_editor') }}">Éditeurs</a>
            </li>
            {% if app.user %}
                {% if is_granted("ROLE_ADMIN") %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('list_user') }}">Utilisateurs</a>
                    </li>

                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('list_platform') }}">Platforms</a>
                    </li>
                {% endif %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('profile') }}">Profil</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
                </li>
            {% endif %}

        </ul>
        <span class="navbar-text">
            {% if app.user %}
                {{ app.user.getLastName }} {{ app.user.getFirstName }}
            {% endif %}
        </span>
    </div>
</nav>